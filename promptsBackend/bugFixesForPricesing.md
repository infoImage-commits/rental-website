first of all we have the Configured Dates
which uses the endpoint 
GET
/api/properties/{propertyId}/daily-prices
the dates we put does not show the full date this endpoint have we should be able to delete as we which also it does not look the best as it would be better if we have a real calander when we hover over a date we should it's price and we should have a custom componet where we use the endpoint for deletling to delele many days at once those are the things that should be added could you try and create a plan for them 